sendData:
    sendData est un script python qui récupère les données du thermomètre et les envoies vers la base de données.
    Le script récupère les données dans un tableau qui est parsé et ensuite envoyé vers la base de données mongobd via l'API Rest de mongobd.